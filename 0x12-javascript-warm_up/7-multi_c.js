#!/usr/bin/node
let word = 'C is fun';
if (parseInt(process.argv[2] == NaN)
	console.log("Missing number of occurrences")
else{
	x = parseInt(process.argv[2]);
	let i = 0;
	while (x < i){
		console.log(word);
		i++;
	}
}
